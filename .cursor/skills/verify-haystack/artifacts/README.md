# verify-haystack artifacts

Proof captures for LongMuch / haystack. This directory survives `helpers/cleanup`.
Never delete these files during teardown. Per-feature subdirectories hold HTML, status codes, and meta.
