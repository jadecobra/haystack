# Empty placeholder previously failed every push (invalid YAML).
name: Lighthouse
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lighthouse not configured"
